const turnDehumidifierOn = async () => {
  // read database
  // if switch already on, exit
  // turn Dehumidifier on
};

const turnDehumidifierOff = async () => {
  // read database
  // if switch already off, exit
  // turn Dehumidifier off
};

module.exports = {
  turnDehumidifierOn,
  turnDehumidifierOff,
};
