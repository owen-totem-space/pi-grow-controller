const turnHumidifierOn = async () => {
  // read database
  // if switch already on, exit
  // turn Humidifier on
};

const turnHumidifierOff = async () => {
  // read database
  // if switch already off, exit
  // turn Humidifier off
};

module.exports = {
  turnHumidifierOn,
  turnHumidifierOff,
};
