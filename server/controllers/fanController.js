const turnFanOn = async () => {
  // read database
  // if switch already on, exit
  // turn Fan on
};

const turnFanOff = async () => {
  // read database
  // if switch already off, exit
  // turn Fan off
};

module.exports = {
  turnFanOn,
  turnFanOff,
};
